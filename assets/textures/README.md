# Texture assets

Seamless runtime texture and mask assets belong here. `noise.svg` is the lightweight Phase 1 grain fallback. Follow the naming, format, fallback, and integration conventions in [VISUAL_DIRECTION.md](../../VISUAL_DIRECTION.md).
